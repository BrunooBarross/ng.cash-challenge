import {
    HeaderContainer,
    HeaderText
} from "./styled";

const Header = () => {
    return (
        <HeaderContainer>
            <HeaderText>Sistema Financeiro</HeaderText>
        </HeaderContainer>
    );
};

export default Header;